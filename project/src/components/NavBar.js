import React from 'react';

const NavBar = () => {
    return(
        <div className='NavBar'>
            <button>Login</button>
            <button>Search</button>
        </div>
    )
}

export default NavBar;